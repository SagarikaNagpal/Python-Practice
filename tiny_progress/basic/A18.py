# Question A18: WAP to find out the area of a triangle.
#A = 1/2 × b × h.

def areaOfTriangle(b,h):
    a = 1 / 2 * b* h
    print('Area', a)
b = float(input('b: '))
h = float(input('h: '))
areaOfTriangle(b,h)
